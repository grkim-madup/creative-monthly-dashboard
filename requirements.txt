# ⚠ Streamlit Cloud는 진입점만 리로드하고 import된 모듈은 컨테이너가 재시작할 때까지
# 옛것을 쓴다. 새 함수를 모듈에 추가하고 진입점에서 import하면 배포판이 ImportError로
# 죽는다(2026-09-02 실제 발생). 이 파일을 한 줄 고쳐 push하면 환경 재빌드로 강제 재시작된다.
streamlit==1.60.0
pandas==3.0.5
numpy==2.5.1
plotly==6.9.0
streamlit-quill==0.0.3
dropbox==12.2.1
google-api-python-client==2.198.0
google-auth==2.56.2
google-cloud-firestore==2.29.0
imageio-ffmpeg==0.6.0
# 재시작 마커: 2026-09-02 insight_draft 추가

# 재시작 마커 — contrast_split/contrast_by_media/render_thumbs 새 심볼 (2026-09-02)

# 재시작 마커 — auth.current_editor / CREATIVE_FIELDS 파생 (2026-09-03)
